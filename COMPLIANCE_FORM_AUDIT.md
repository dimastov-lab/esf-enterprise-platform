# Сверка публикуемой формы с оригиналом ГНС КР (STI-007 / Приложение 3)

**Кто:** служба безопасности/комплаенс.
**Что сверяли:** наша финальная публикуемая форма (`mode=view`/`public`/`pdf`)
против оригинала ГНС.
**Эталон:** `Копия 6.pdf` — реальный опубликованный ЭСФ (текстовый слой извлечён
и сличён со строками шаблона `app/templates/esf/form.html` и сериализатором
`app/services/esf_service.py`).

---

## ✅ Совпадает (проверено по тексту эталона)
- Шапка: «BLANK STI - 007», «Кыргызская Республика», «СЧЕТ-ФАКТУРА»,
  «в виде электронного документа на товары», «Приложение 3»,
  «НОМЕР ТЕКУЩЕГО ЛИСТА: 1».
- Строка статуса: **101** «СТАТУС: первоначальный (Принят)», **102** «НОМЕР:»
  (формат `000{ГГГГ}-004-{8}`), **103** «Дата оформления:» (боксы день/месяц/год).
- Раздел 1 «Реквизиты поставщика и покупателя»: коды и подписи **201–208**
  (Поставщик ИНН, Ф.И.О./Наименование, Филиал ИНН, Наименование филиала, Адрес,
  Код и наименование налогового органа, Наименование банка и код (БИК), Счёт в
  банке) и зеркально **301–308** (в т.ч. **306** «…органа/страны:»).
- Раздел 2 «Информация о реализации»: **401–407** (Дата поставки, Вид поставки,
  Форма оплаты, Примечание, Договор…, Корректировка к счёту-фактуре, Причина
  корректировки).
- Раздел 3 «Информация о товаре»: «Код валюты», «Курс валюты»,
  «Товар, подлежащий прослеживаемости».
- Таблица товаров: все 13 колонок и подзаголовки НДС/НсП (Ставка/Сумма).
- Итоги: «Итого по листу:», «Итого по счёту-фактуре:», «Итого в иностранной
  валюте:» (с отметками X).
- Подвал: **450** «Ф.И.О. руководителя организации или индивидуального
  предпринимателя или бухгалтера или налогового представителя», QR в нижней
  левой ячейке, вертикальная отметка даты/времени справа.

## 🔴 Расхождения — найдены и ИСПРАВЛЕНЫ
| # | Поле | Оригинал ГНС | Было у нас | Исправление |
|---|------|--------------|-----------|-------------|
| 1 | Цена за единицу | `116.49850` (5 знаков) | сохранялся масштаб ввода (`100` → `100`) | всегда 5 знаков: `_qty5()` → `100.00000` |
| 2 | Количество (объём) | `21000.00000` (5 знаков) | то же | всегда 5 знаков: `_qty5()` |
| 3 | Метка времени (подвал) | время через точки `13.46.33` | через двоеточия `13:46:33` | формат `%d/%m/%Y %H.%M.%S` |

Закреплено тестом `test_published_form_matches_gns_formatting` (5 знаков у
цены/количества; `дд/мм/гггг чч.мм.сс`; двоеточий во времени нет).

## ✅ Закрыто по чистому эталону (изображение формы ГНС)
- **НДС/НсП «Ставка»** — на чётком эталоне в колонках «Ставка» стоит **`0`**
  (а «Сумма» — `0.00`). Ранее в текстовом слое мелькало `0.000` — это был артефакт
  извлечения. Наш `_rate` уже печатает `0` → **совпадает**, правка не нужна.
- Построчная сверка с чистым эталоном (коды 101–103, 201–208/301–308 с боксами
  ИНН, 206 с боксами кода `[0 0 4]` + наименование и 306 без боксов «643 -
  Российская Федерация», 401–407 с боксами дат, таблица товаров и три строки
  «Итого» с отметками X, подвал 450 + QR + вертикальная метка `29/05/2026 13.46.33`)
  — **расхождений не выявлено**.

> Примечание: живой side-by-side рендер не делал — dev-БД (PostgreSQL) была
> остановлена. Соответствие форматирования подтверждено проходящим тестом
> `test_published_form_matches_gns_formatting`.

## Замечание об иммутабельности
Изменения формата применяются к **новым** публикациям. Уже опубликованные
документы печатаются из своих неизменяемых снапшотов и сохраняют прежний формат —
это корректно (снапшот = источник истины на момент публикации).

## Итог
Структура, коды, подписи и значения формы **полностью соответствуют** оригиналу
ГНС. Устранены три расхождения форматирования (цена, количество, время); пункт по
«Ставке» НДС/НсП **закрыт** по чистому эталону (`0` — совпадает). Открытых
расхождений нет. Регрессия: **45 тестов зелёные.**
